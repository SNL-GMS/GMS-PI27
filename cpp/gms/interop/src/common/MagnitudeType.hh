#ifndef MAGNITUDE_TYPE_H
#define MAGNITUDE_TYPE_H

enum class MagnitudeType {
    MB = 0,
    MB_CODA = 1,
    MB_MB = 2,
    MB_MLE = 3,
    MB_PG = 4,
    MB_REL_T = 5,
    ML = 6,
    MS = 7,
    MS_MLE = 8,
    MS_VMAX = 9,
    MW_CODA = 10
};

#endif // MAGNITUDE_TYPE_H